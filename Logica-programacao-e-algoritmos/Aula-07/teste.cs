using System.Collections.Concurrent;
using static System.Console;
string senha, aula, entrada, correto, Incorreto;
// entrada de dados//
WriteLine("digite a senha");
entrada = (ReadLine());
aula = "123";
senha = aula;
// processamento de dados//
if (entrada = aula)
{
    senha == Correto;
}
else if (entrada != aula )
{
    senha == incorreto;
}
WriteLine($"a sua senha esta {senha}");